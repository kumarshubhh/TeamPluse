# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration
# Local MongoDB: mongodb://localhost:27017/TeamPulse
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/TeamPulse
MONGODB_URI=mongodb://localhost:27017/TeamPulse

# JWT Configuration
# Generate a strong secret key for production (min 10 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-10-chars
JWT_EXPIRES_IN=1h

# CORS Configuration
# For development: http://localhost:5173
# For production: https://yourdomain.com
# Use * for allowing all origins (not recommended for production)
CORS_ORIGIN=http://localhost:5173

# Socket.IO Configuration
SOCKET_PATH=/socket.io

# Logging Configuration (Optional)
# LOG_LEVEL=info (debug, info, warn, error)
